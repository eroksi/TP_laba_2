#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <stdexcept>

using namespace std;

class FileManager{
    public:
        static void reverseSentences(const string& filename);
};

